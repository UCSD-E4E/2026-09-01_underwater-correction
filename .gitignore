# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.venv/

# Harness outputs. All of these are regenerable from the manifest + the NAS,
# and the CSVs carry per-image label geometry that has no business in git.
*.csv
raw_cache/
jpeg_cache/
looks/
web/
*.html

# Trial output: 254 full-resolution JPEGs (~1.8 GB) plus its allocation.
# Regenerable from the backlog query and the seed.
trial/

# Repo-local install of bm3d (uv pip install --target .pylib bm3d); see README.
.pylib/

# Frame lists carry per-image label geometry; the ids alone are committed in data/frame_sets.json.
data/*.csv
data/crops/

data/*.log
